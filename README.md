# site-map

The tool expects url of a site and lists its pages.

## Example usage
`java -jar target/sitemap.jar https://github.com`

This should output newline separated list all existing urls `github.com` serves.
